import { makeStyles } from '@material-ui/core';

export const useHeaderStyles = makeStyles({
  root: {
    height: `80px`,
    width: `100%`,
    position: `fixed`,
    left: 0,
    right: 0,
    top: 0,
    background: ``,
  },
  drawer: {
    width: `200px`,
  },
});
